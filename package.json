{
  "name": "browser-git-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight browser Git workspace SDK powered by libgit2 WASM.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "postinstall": "node scripts/copy-wasm-git.mjs",
    "dev": "npm run dev --prefix demo",
    "build:demo": "npm run build --prefix demo",
    "test": "tsx tests/diff.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "wasm-git": "^0.0.17"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
